diff --git a/components/ungoogled/ungoogled_switches.cc b/components/ungoogled/ungoogled_switches.cc
index f88df9a..8f2ed75 100644
--- a/components/ungoogled/ungoogled_switches.cc
+++ b/components/ungoogled/ungoogled_switches.cc
@@ -42,6 +42,10 @@ const char kFingerprintHardwareConcurrency[] = "fingerprint-hardware-concurrency
 // Specify the GPU model reported on macOS, e.g. "M5" or "A18 Pro"
 const char kFingerprintGpuModel[] = "fingerprint-gpu-model";
 
+// Specify navigator.deviceMemory and the Device-Memory client hints in GB
+// (2, 4, 8, 16 or 32). third_party/blink/common uses this name as a literal.
+const char kFingerprintDeviceMemory[] = "fingerprint-device-memory";
+
 // Specify the operating system type
 const char kFingerprintPlatform[] = "fingerprint-platform";
 
diff --git a/components/ungoogled/ungoogled_switches.h b/components/ungoogled/ungoogled_switches.h
index 6cdc329..b26a20d 100644
--- a/components/ungoogled/ungoogled_switches.h
+++ b/components/ungoogled/ungoogled_switches.h
@@ -24,6 +24,7 @@ COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintScreenHeight[];
 COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintDeviceScaleFactor[];
 COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintHardwareConcurrency[];
 COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintGpuModel[];
+COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintDeviceMemory[];
 COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintPlatform[];
 COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintPlatformVersion[];
 COMPONENT_EXPORT(UNGOOGLED) extern const char kFingerprintLocation[];
diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index ed43ede..d6f4201 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -3911,6 +3911,7 @@ void RenderProcessHostImpl::PropagateBrowserCommandLineToRenderer(
       switches::kFingerprintDeviceScaleFactor,
       switches::kFingerprintHardwareConcurrency,
       switches::kFingerprintGpuModel,
+      switches::kFingerprintDeviceMemory,
       switches::kFingerprintPlatform,
       switches::kFingerprintPlatformVersion,
       switches::kFingerprintLocation,
diff --git a/third_party/blink/common/device_memory/approximated_device_memory.cc b/third_party/blink/common/device_memory/approximated_device_memory.cc
index 7fd53eb..16b2d4d 100644
--- a/third_party/blink/common/device_memory/approximated_device_memory.cc
+++ b/third_party/blink/common/device_memory/approximated_device_memory.cc
@@ -6,7 +6,9 @@
 
 #include "base/byte_size.h"
 #include "base/check_op.h"
+#include "base/command_line.h"
 #include "base/feature_list.h"
+#include "base/strings/string_number_conversions.h"
 #include "base/system/sys_info.h"
 #include "third_party/blink/public/common/features.h"
 
@@ -23,6 +25,28 @@ void ApproximatedDeviceMemory::Initialize() {
   DCHECK_EQ(0, physical_memory_mb_);
   physical_memory_mb_ = ::base::SysInfo::AmountOfTotalPhysicalMemory().InMiB();
   CalculateAndSetApproximatedDeviceMemory();
+
+  // Fingerprint profiles report one value everywhere: navigator.deviceMemory
+  // and the Device-Memory client hints (browser and renderer both call this).
+  // The switch names are literals because blink/common can't depend on
+  // //components; see components/ungoogled/ungoogled_switches.cc.
+  if (!base::CommandLine::InitializedForCurrentProcess()) {
+    return;
+  }
+  const base::CommandLine* command_line =
+      base::CommandLine::ForCurrentProcess();
+  if (!command_line->HasSwitch("fingerprint")) {
+    return;
+  }
+  int memory_gb = 0;
+  if (!base::StringToInt(
+          command_line->GetSwitchValueASCII("fingerprint-device-memory"),
+          &memory_gb) ||
+      (memory_gb != 2 && memory_gb != 4 && memory_gb != 8 && memory_gb != 16 &&
+       memory_gb != 32)) {
+    memory_gb = 8;
+  }
+  approximated_device_memory_gb_ = static_cast<float>(memory_gb);
 }
 
 // static
diff --git a/third_party/blink/renderer/core/dom/document.cc b/third_party/blink/renderer/core/dom/document.cc
index d17d3c1..690dea5 100644
--- a/third_party/blink/renderer/core/dom/document.cc
+++ b/third_party/blink/renderer/core/dom/document.cc
@@ -35,7 +35,6 @@
 #include <utility>
 
 #include "base/auto_reset.h"
-#include "base/command_line.h"
 #include "base/containers/adapters.h"
 #include "base/dcheck_is_on.h"
 #include "base/debug/dump_without_crashing.h"
@@ -50,7 +49,6 @@
 #include "cc/animation/animation_timeline.h"
 #include "cc/input/overscroll_behavior.h"
 #include "cc/input/scroll_snap_data.h"
-#include "components/ungoogled/ungoogled_switches.h"
 #include "mojo/public/cpp/bindings/lib/wtf_hash_util.h"
 #include "mojo/public/cpp/bindings/pending_remote.h"
 #include "mojo/public/cpp/bindings/remote.h"
@@ -1054,23 +1052,10 @@ Document::Document(const DocumentInit& initializer,
       data_(MakeGarbageCollected<DocumentData>(GetExecutionContext())) {
   TRACE_EVENT("blink", "Document::Document", perfetto::Flow::FromPointer(this));
   DCHECK(agent_);
-    const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint)) {
-    std::string seed_str = command_line->GetSwitchValueASCII(switches::kFingerprint);
-    // 为x和y使用不同的组合字符串
-    std::string combined_x = seed_str + "offset_x";
-    std::string combined_y = seed_str + "offset_y";
-
-    uint32_t hash_val_x = std::hash<std::string>{}(combined_x);
-    uint32_t hash_val_y = std::hash<std::string>{}(combined_y);
-
-    // 映射到[-0.5, 0.5]范围
-    double norm_x = (hash_val_x & 0xFFFFFFFF) / 4294967295.0 - 0.5;
-    double norm_y = (hash_val_y & 0xFFFFFFFF) / 4294967295.0 - 0.5;
-
-    // 计算很小的偏移量：±0.001像素
-    noise_factor_x_ = norm_x * 0.002;
-    noise_factor_y_ = norm_y * 0.002;
+  if (RuntimeEnabledFeatures::FingerprintingClientRectsNoiseEnabled()) {
+    // Precompute -0.0003% to 0.0003% noise factor for get*ClientRect*() fingerprinting
+    noise_factor_x_ = 1 + (base::RandDouble() - 0.5) * 0.000003;
+    noise_factor_y_ = 1 + (base::RandDouble() - 0.5) * 0.000003;
   }
   if (base::FeatureList::IsEnabled(features::kDelayAsyncScriptExecution) &&
       features::kDelayAsyncScriptExecutionDelayByDefaultParam.Get()) {
diff --git a/third_party/blink/renderer/core/dom/element.cc b/third_party/blink/renderer/core/dom/element.cc
index bf2648b..361f4f6 100644
--- a/third_party/blink/renderer/core/dom/element.cc
+++ b/third_party/blink/renderer/core/dom/element.cc
@@ -3383,16 +3383,6 @@ DOMRectList* Element::getClientRects() {
     GetDocument().AdjustRectForScrollAndAbsoluteZoom(rect,
                                                      *element_layout_object);
   }
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("clientrects") == std::string::npos)) {
-    if (!ShouldSkipClientRectsOffset()) {
-      for (auto& rect : rects) {
-        rect.Offset(GetDocument().GetNoiseFactorX(), GetDocument().GetNoiseFactorY());
-      }
-    }
-  }
   return MakeGarbageCollected<DOMRectList>(rects);
 }
 
@@ -3442,13 +3432,8 @@ gfx::RectF Element::GetBoundingClientRectNoLifecycleUpdate() const {
   DCHECK(element_layout_object);
   GetDocument().AdjustRectForScrollAndAbsoluteZoom(result,
                                                    *element_layout_object);
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("clientrects") == std::string::npos)) {
-    if (!ShouldSkipClientRectsOffset()) {
-      result.Offset(GetDocument().GetNoiseFactorX(), GetDocument().GetNoiseFactorY());
-    }
+  if (RuntimeEnabledFeatures::FingerprintingClientRectsNoiseEnabled()) {
+    result.Scale(GetDocument().GetNoiseFactorX(), GetDocument().GetNoiseFactorY());
   }
   return result;
 }
diff --git a/third_party/blink/renderer/core/dom/range.cc b/third_party/blink/renderer/core/dom/range.cc
index 9e5ca69..63a3a1e 100644
--- a/third_party/blink/renderer/core/dom/range.cc
+++ b/third_party/blink/renderer/core/dom/range.cc
@@ -23,8 +23,6 @@
  * Boston, MA 02110-1301, USA.
  */
 
-#include "base/command_line.h"
-#include "components/ungoogled/ungoogled_switches.h"
 #include "third_party/blink/renderer/core/dom/range.h"
 
 #include "third_party/blink/renderer/core/display_lock/display_lock_document_state.h"
@@ -1691,12 +1689,9 @@ DOMRectList* Range::getClientRects() const {
   Vector<gfx::QuadF> quads;
   GetBorderAndTextQuads(quads);
 
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("clientrects") == std::string::npos)) {
+  if (RuntimeEnabledFeatures::FingerprintingClientRectsNoiseEnabled()) {
     for (gfx::QuadF& quad : quads) {
-      quad.Offset(owner_document_->GetNoiseFactorX(), owner_document_->GetNoiseFactorY());
+      quad.Scale(owner_document_->GetNoiseFactorX(), owner_document_->GetNoiseFactorY());
     }
   }
 
@@ -1708,11 +1703,8 @@ DOMRect* Range::getBoundingClientRect() const {
   // impact is understood.
   SyncScrollAttemptHeuristic::DidAccessScrollOffset();
   auto rect = BoundingRect();
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("clientrects") == std::string::npos)) {
-    rect.Offset(owner_document_->GetNoiseFactorX(), owner_document_->GetNoiseFactorY());
+  if (RuntimeEnabledFeatures::FingerprintingClientRectsNoiseEnabled()) {
+    rect.Scale(owner_document_->GetNoiseFactorX(), owner_document_->GetNoiseFactorY());
   }
   return DOMRect::FromRectF(rect);
 }
diff --git a/third_party/blink/renderer/core/frame/navigator_concurrent_hardware.cc b/third_party/blink/renderer/core/frame/navigator_concurrent_hardware.cc
index da69d2f..7a97c15 100644
--- a/third_party/blink/renderer/core/frame/navigator_concurrent_hardware.cc
+++ b/third_party/blink/renderer/core/frame/navigator_concurrent_hardware.cc
@@ -4,7 +4,11 @@
 
 #include "third_party/blink/renderer/core/frame/navigator_concurrent_hardware.h"
 
+#include <functional>
+#include <string>
+
 #include "base/command_line.h"
+#include "base/strings/string_number_conversions.h"
 #include "base/system/sys_info.h"
 #include "components/ungoogled/ungoogled_switches.h"
 
@@ -13,15 +17,24 @@ namespace blink {
 unsigned NavigatorConcurrentHardware::hardwareConcurrency() const {
   const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
 
-  if (command_line->HasSwitch(switches::kFingerprintHardwareConcurrency)) {
-    std::string value = command_line->GetSwitchValueASCII(switches::kFingerprintHardwareConcurrency);
-    unsigned long custom_concurrency = std::stoul(value);
-    return static_cast<unsigned>(custom_concurrency);
+  // Invalid or out-of-range values are treated as if the switch were absent
+  // (std::stoul would abort the renderer on non-numeric input).
+  unsigned custom_concurrency = 0;
+  if (command_line->HasSwitch(switches::kFingerprintHardwareConcurrency) &&
+      base::StringToUint(command_line->GetSwitchValueASCII(
+                             switches::kFingerprintHardwareConcurrency),
+                         &custom_concurrency) &&
+      custom_concurrency >= 1 && custom_concurrency <= 256) {
+    return custom_concurrency;
   }
 
   if (command_line->HasSwitch(switches::kFingerprint)) {
     std::string fingerprint_str = command_line->GetSwitchValueASCII(switches::kFingerprint);
-    uint32_t fingerprint = static_cast<uint32_t>(std::stoull(fingerprint_str));
+    uint64_t parsed = 0;
+    uint32_t fingerprint =
+        base::StringToUint64(fingerprint_str, &parsed)
+            ? static_cast<uint32_t>(parsed)
+            : static_cast<uint32_t>(std::hash<std::string>{}(fingerprint_str));
     return static_cast<unsigned>(((fingerprint % 13) + 4) * 2);
   }
 
diff --git a/third_party/blink/renderer/core/frame/navigator_device_memory.cc b/third_party/blink/renderer/core/frame/navigator_device_memory.cc
index 792ee43..05fa33e 100644
--- a/third_party/blink/renderer/core/frame/navigator_device_memory.cc
+++ b/third_party/blink/renderer/core/frame/navigator_device_memory.cc
@@ -12,8 +12,7 @@
 namespace blink {
 
 float NavigatorDeviceMemory::deviceMemory() const {
-  return 8;
-  // return ApproximatedDeviceMemory::GetApproximatedDeviceMemory();
+  return ApproximatedDeviceMemory::GetApproximatedDeviceMemory();
 }
 
 }  // namespace blink
diff --git a/third_party/blink/renderer/core/html/canvas/canvas_async_blob_creator.cc b/third_party/blink/renderer/core/html/canvas/canvas_async_blob_creator.cc
index adb616d..5a7e7c0 100644
--- a/third_party/blink/renderer/core/html/canvas/canvas_async_blob_creator.cc
+++ b/third_party/blink/renderer/core/html/canvas/canvas_async_blob_creator.cc
@@ -4,10 +4,13 @@
 
 #include "third_party/blink/renderer/core/html/canvas/canvas_async_blob_creator.h"
 
+#include "base/command_line.h"
 #include "base/location.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
 #include "build/build_config.h"
+#include "components/ungoogled/ungoogled_switches.h"
+#include "partition_alloc/partition_alloc_constants.h"
 #include "third_party/blink/public/platform/platform.h"
 #include "third_party/blink/public/platform/task_type.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
@@ -18,6 +21,7 @@
 #include "third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h"
 #include "third_party/blink/renderer/platform/graphics/image_data_buffer.h"
 #include "third_party/blink/renderer/platform/graphics/skia/skia_utils.h"
+#include "third_party/blink/renderer/platform/graphics/static_bitmap_image.h"
 #include "third_party/blink/renderer/platform/graphics/unaccelerated_static_bitmap_image.h"
 #include "third_party/blink/renderer/platform/heap/garbage_collected.h"
 #include "third_party/blink/renderer/platform/image-encoders/image_encoder_utils.h"
@@ -32,6 +36,8 @@
 #include "third_party/blink/renderer/platform/wtf/functional.h"
 #include "third_party/blink/renderer/platform/wtf/text/base64.h"
 #include "third_party/blink/renderer/platform/wtf/text/strcat.h"
+#include "third_party/skia/include/core/SkData.h"
+#include "third_party/skia/include/core/SkImage.h"
 #include "third_party/skia/include/core/SkSurface.h"
 #include "third_party/skia/include/encode/SkPngRustEncoder.h"
 
@@ -135,6 +141,53 @@ void RecordScaledDurationHistogram(ImageEncodingMimeType mime_type,
   }
 }
 
+// Fingerprint canvas noise for toBlob/convertToBlob: returns a noised copy of
+// |image|, made the same way as in HTMLCanvasElement::ToDataURLInternal (8-bit
+// sources only, colour space and opaque alpha kept). Returns |image| unchanged
+// when noise is off, for other colour types, or on any failure.
+scoped_refptr<StaticBitmapImage> MaybeApplyFingerprintCanvasNoise(
+    scoped_refptr<StaticBitmapImage> image) {
+  const base::CommandLine* command_line =
+      base::CommandLine::ForCurrentProcess();
+  if (!command_line->HasSwitch(switches::kFingerprint) ||
+      (command_line->HasSwitch(switches::kDisableSpoofing) &&
+       command_line->GetSwitchValueASCII(switches::kDisableSpoofing)
+               .find("canvas") != std::string::npos)) {
+    return image;
+  }
+  PaintImage paint_image = image->PaintImageForCurrentFrame();
+  const SkImageInfo src_info = paint_image.GetSkImageInfo();
+  if (src_info.colorType() != kRGBA_8888_SkColorType &&
+      src_info.colorType() != kBGRA_8888_SkColorType) {
+    return image;
+  }
+  const SkImageInfo info =
+      src_info.makeColorType(kRGBA_8888_SkColorType)
+          .makeAlphaType(src_info.alphaType() == kOpaque_SkAlphaType
+                             ? kOpaque_SkAlphaType
+                             : kUnpremul_SkAlphaType);
+  const size_t row_bytes = info.minRowBytes();
+  const size_t size = info.computeMinByteSize();
+  if (size == 0 || SkImageInfo::ByteSizeOverflowed(size) ||
+      size > partition_alloc::MaxDirectMapped()) {
+    return image;
+  }
+  sk_sp<SkData> data = SkData::MakeUninitialized(size);
+  if (!data ||
+      !paint_image.readPixels(info, data->writable_data(), row_bytes, 0, 0)) {
+    return image;
+  }
+  StaticBitmapImage::ShuffleSubchannelColorData(data->writable_data(), info, 0,
+                                                0);
+  sk_sp<SkImage> noised =
+      SkImages::RasterFromData(info, std::move(data), row_bytes);
+  if (!noised) {
+    return image;
+  }
+  return UnacceleratedStaticBitmapImage::Create(std::move(noised),
+                                                image->Orientation());
+}
+
 }  // anonymous namespace
 
 CanvasAsyncBlobCreator::CanvasAsyncBlobCreator(
@@ -180,6 +233,9 @@ CanvasAsyncBlobCreator::CanvasAsyncBlobCreator(
   image_ = image->MakeUnaccelerated();
 
   if (image_) {
+    // Replace the image before |skia_image_| is derived from it, so the lazy
+    // decode, peekPixels and every encoder see the noised copy.
+    image_ = MaybeApplyFingerprintCanvasNoise(std::move(image_));
     skia_image_ = image_->PaintImageForCurrentFrame().GetSwSkImage();
     CHECK(skia_image_);
     CHECK(!skia_image_->isTextureBacked());
diff --git a/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc b/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
index f521038..41ce017 100644
--- a/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
+++ b/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
@@ -1290,47 +1290,41 @@ String HTMLCanvasElement::ToDataURLInternal(
   scoped_refptr<StaticBitmapImage> image_bitmap = Snapshot(source_buffer);
   if (image_bitmap) {
 
-    // 添加指纹保护：在创建 ImageDataBuffer 之前修改像素数据副本
+    // Fingerprint canvas noise: encode from a noised copy of the snapshot. The
+    // copy is only made for 8-bit sources and keeps the colour space (JPEG and
+    // WebP embed it as an ICC profile) and opaque alpha; anything else, or any
+    // failure, encodes the original snapshot unmodified.
     const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
     if (command_line->HasSwitch(switches::kFingerprint) &&
         (!command_line->HasSwitch(switches::kDisableSpoofing) ||
          command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("canvas") == std::string::npos)) {
-
-      // 获取图像的像素数据进行修改（创建完全独立的副本）
-      SkImageInfo image_info = SkImageInfo::Make(
-          image_bitmap->width(),
-          image_bitmap->height(),
-          kRGBA_8888_SkColorType,  // 使用标准的RGBA格式
-          kUnpremul_SkAlphaType);
-
-      // 创建独立的像素缓冲区副本
-      size_t row_bytes = image_info.minRowBytes();
-      size_t buffer_size = image_info.computeMinByteSize();
-
-      if (buffer_size > 0 && buffer_size <= partition_alloc::MaxDirectMapped()) {
-        std::vector<uint8_t> pixel_buffer(buffer_size);
-
-        // 从原始图像读取像素数据到我们的副本中
-        PaintImage original_paint_image = image_bitmap->PaintImageForCurrentFrame();
-        if (original_paint_image.readPixels(image_info, pixel_buffer.data(), row_bytes, 0, 0)) {
-
-          // 对副本应用指纹保护修改（不影响原始数据）
-          StaticBitmapImage::ShuffleSubchannelColorData(
-              pixel_buffer.data(), image_info, 0, 0);
-
-          // 用修改后的像素数据创建完全新的图像对象
-          sk_sp<SkData> modified_data = SkData::MakeWithCopy(pixel_buffer.data(), buffer_size);
-          if (modified_data) {
+      PaintImage original_paint_image = image_bitmap->PaintImageForCurrentFrame();
+      const SkImageInfo src_info = original_paint_image.GetSkImageInfo();
+      if (src_info.colorType() == kRGBA_8888_SkColorType ||
+          src_info.colorType() == kBGRA_8888_SkColorType) {
+        const SkImageInfo image_info =
+            src_info.makeColorType(kRGBA_8888_SkColorType)
+                .makeAlphaType(src_info.alphaType() == kOpaque_SkAlphaType
+                                   ? kOpaque_SkAlphaType
+                                   : kUnpremul_SkAlphaType);
+        const size_t row_bytes = image_info.minRowBytes();
+        const size_t buffer_size = image_info.computeMinByteSize();
+        if (buffer_size > 0 && !SkImageInfo::ByteSizeOverflowed(buffer_size) &&
+            buffer_size <= partition_alloc::MaxDirectMapped()) {
+          sk_sp<SkData> pixel_data = SkData::MakeUninitialized(buffer_size);
+          if (pixel_data &&
+              original_paint_image.readPixels(image_info,
+                                              pixel_data->writable_data(),
+                                              row_bytes, 0, 0)) {
+            StaticBitmapImage::ShuffleSubchannelColorData(
+                pixel_data->writable_data(), image_info, 0, 0);
             sk_sp<SkImage> modified_sk_image = SkImages::RasterFromData(
-                image_info, modified_data, row_bytes);
-
+                image_info, std::move(pixel_data), row_bytes);
             if (modified_sk_image) {
-              // 创建新的 StaticBitmapImage，使用通用的创建方法
               PaintImage modified_paint_image = PaintImageBuilder::WithDefault()
                   .set_id(PaintImage::GetNextId())
                   .set_image(modified_sk_image, PaintImage::GetNextContentId())
                   .TakePaintImage();
-
               image_bitmap = StaticBitmapImage::Create(
                   modified_paint_image, image_bitmap->Orientation());
             }
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
index 41f6f30..3de2131 100644
--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
@@ -524,7 +524,20 @@ ImageData* BaseRenderingContext2D::getImageDataInternal(
     if (read_pixels_successful && command_line->HasSwitch(switches::kFingerprint) &&
         (!command_line->HasSwitch(switches::kDisableSpoofing) ||
          command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("canvas") == std::string::npos)) {
-      StaticBitmapImage::ShuffleSubchannelColorData(image_data_pixmap.addr(), image_data_pixmap.info(), sx, sy);
+      // Noise the whole ImageData keyed by its canvas offset. Out-of-canvas
+      // pixels are zero-initialized (alpha 0), which the noise treats like the
+      // canvas edge, so an oversized read matches a full read. Only 8-bit
+      // canvases are noised, the same set as toDataURL/toBlob.
+      const SkColorType snapshot_color_type =
+          snapshot->PaintImageForCurrentFrame().GetSkImageInfo().colorType();
+      gfx::Rect overlap = image_data_rect;
+      overlap.Intersect(snapshot_rect);
+      if ((snapshot_color_type == kRGBA_8888_SkColorType ||
+           snapshot_color_type == kBGRA_8888_SkColorType) &&
+          overlap.width() >= 8 && overlap.height() >= 8) {
+        StaticBitmapImage::ShuffleSubchannelColorData(
+            image_data_pixmap.addr(), image_data_pixmap.info(), sx, sy);
+      }
     }
   }
 
@@ -1216,28 +1229,6 @@ TextMetrics* BaseRenderingContext2D::measureText(const String& text) {
       font, direction, state.GetTextBaseline(), state.GetTextAlign(), text,
       host->GetPlainTextPainter());
 
-  // Scale text metrics if enabled
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("canvas") == std::string::npos)) {
-    std::string seed_str = command_line->GetSwitchValueASCII(switches::kFingerprint);
-    std::string combined_rects_x = seed_str + "clientrects_noise_x";
-    uint32_t hash_val_x = std::hash<std::string>{}(combined_rects_x);
-     // 映射到[-0.5, 0.5]范围
-    double norm_x = (hash_val_x & 0xFFFFFFFF) / 4294967295.0 - 0.5;
-    // 使用 0.00001 作为因子 (相当于原来的 1/100000.0)
-    double noise_x = norm_x * 0.00001;
-    if (HostAsOffscreenCanvas()) {
-      if (auto* window = DynamicTo<LocalDOMWindow>(GetTopExecutionContext())) {
-        if (window->GetFrame() && window->GetFrame()->GetDocument() && text.length() > 0) {
-          text_metrics->Shuffle(noise_x);
-        }
-      }
-    } else if (canvas && text.length() > 0) {
-      text_metrics->Shuffle(noise_x);
-    }
-  }
   return text_metrics;
 }
 
diff --git a/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc b/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
index 418c2c3..0aa6672 100644
--- a/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
+++ b/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
@@ -180,8 +180,11 @@ namespace {
 // Deterministic per-sample noise for --fingerprint. This replaces the old
 // ±0.01 Hz sample-rate noise, which float32 rounded to ~7 values (one of them
 // no noise at all) and which also changed buffer.sampleRate. Each sample is
-// scaled by 1 ± (0.5..1.0)e-6, derived from the seed, channel and sample index,
-// so the factor is never exactly 1.
+// scaled by 1 ± (0.5..1.0)e-6, derived from the seed and sample index, so the
+// factor is never exactly 1. Samples equal to their original neighbour (flat
+// runs: constant sources, DC offsets, silence, square-wave plateaus) are left
+// exact, and the channel isn't part of the key, so identical channels stay
+// identical.
 void ApplyFingerprintAudioNoise(AudioBuffer* buffer) {
   const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
   if (!command_line->HasSwitch(switches::kFingerprint)) {
@@ -205,9 +208,18 @@ void ApplyFingerprintAudioNoise(AudioBuffer* buffer) {
       continue;
     }
     base::span<float> samples = data->AsSpan();
-    for (size_t i = 0; i < samples.size(); ++i) {
-      // splitmix64 over (key, channel, index)
-      uint64_t z = key ^ (static_cast<uint64_t>(channel) << 48) ^ i;
+    const size_t count = samples.size();
+    float previous = 0.0f;  // original (un-noised) value of samples[i - 1]
+    for (size_t i = 0; i < count; ++i) {
+      const float original = samples[i];
+      const bool flat = (i > 0 && original == previous) ||
+                        (i + 1 < count && original == samples[i + 1]);
+      previous = original;
+      if (flat) {
+        continue;
+      }
+      // splitmix64 over (key, index)
+      uint64_t z = key ^ i;
       z += 0x9e3779b97f4a7c15ULL;
       z = (z ^ (z >> 30)) * 0xbf58476d1ce4e5b9ULL;
       z = (z ^ (z >> 27)) * 0x94d049bb133111ebULL;
@@ -215,7 +227,7 @@ void ApplyFingerprintAudioNoise(AudioBuffer* buffer) {
       const double magnitude =
           0.5 + (static_cast<double>(z >> 11) / 9007199254740992.0) * 0.5;
       const double factor = 1.0 + ((z & 1) ? magnitude : -magnitude) * 1e-6;
-      samples[i] = static_cast<float>(samples[i] * factor);
+      samples[i] = static_cast<float>(original * factor);
     }
   }
 }
diff --git a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
index 2be224d..f20eabe 100644
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -25,10 +25,12 @@
 
 #include "third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.h"
 
+#include <algorithm>
 #include <inttypes.h>
 
 #include <memory>
 #include <utility>
+#include <vector>
 
 #include "base/bit_cast.h"
 #include "base/byte_size.h"
@@ -5372,31 +5374,65 @@ void WebGLRenderingContextBase::ReadPixelsHelper(GLint x,
     }
     ContextGL()->ReadPixels(x, y, width, height, format, type, data);
 
-    // 添加指纹保护：修改已读取的像素数据
+    // Fingerprint canvas noise for readPixels. Only RGBA/UNSIGNED_BYTE reads
+    // whose row stride is exactly 4 * width are touched (other formats use 1-2
+    // bytes or 16-bit components per pixel). The noise runs on a tight copy of
+    // the part of the read that overlaps the framebuffer. On the default
+    // framebuffer the copy is top-down and keyed by canvas coordinates, so it
+    // matches the 2D noise of the same pixels. Texture-backed framebuffers are
+    // skipped because their size isn't tracked.
     const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-    if (command_line->HasSwitch(switches::kFingerprint) &&
+    if (format == GL_RGBA && type == GL_UNSIGNED_BYTE && data && width > 0 &&
+        height > 0 && pack_alignment_ > 0 &&
+        (4 * static_cast<int64_t>(width)) % pack_alignment_ == 0 &&
+        command_line->HasSwitch(switches::kFingerprint) &&
         (!command_line->HasSwitch(switches::kDisableSpoofing) ||
          command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("canvas") == std::string::npos)) {
-      // 构建 SkImageInfo 来描述像素格式
-      SkColorType color_type;
-      SkAlphaType alpha_type = kUnpremul_SkAlphaType;
-
-      // 根据 WebGL 格式映射到 Skia 格式
-      if (format == GL_RGBA && type == GL_UNSIGNED_BYTE) {
-        color_type = kRGBA_8888_SkColorType;
-      } else if (format == GL_RGB && type == GL_UNSIGNED_BYTE) {
-        color_type = kRGB_888x_SkColorType;
-      } else if (format == GL_ALPHA && type == GL_UNSIGNED_BYTE) {
-        color_type = kAlpha_8_SkColorType;
-      } else {
-        // 对于其他格式，假设为 RGBA
-        color_type = kRGBA_8888_SkColorType;
+      int64_t fb_width = -1;
+      int64_t fb_height = -1;
+      if (!framebuffer) {
+        fb_width = drawingBufferWidth();
+        fb_height = drawingBufferHeight();
+      } else if (WebGLObject* attachment = framebuffer->GetAttachmentObject(
+                     framebuffer->GetReadBuffer());
+                 attachment && attachment->IsRenderbuffer()) {
+        auto* renderbuffer = static_cast<WebGLRenderbuffer*>(attachment);
+        fb_width = renderbuffer->Width();
+        fb_height = renderbuffer->Height();
+      }
+      const int64_t x0 = std::max<int64_t>(x, 0);
+      const int64_t y0 = std::max<int64_t>(y, 0);
+      const int64_t ow =
+          std::min<int64_t>(static_cast<int64_t>(x) + width, fb_width) - x0;
+      const int64_t oh =
+          std::min<int64_t>(static_cast<int64_t>(y) + height, fb_height) - y0;
+      if (fb_width > 0 && fb_height > 0 && ow >= 8 && oh >= 8) {
+        const bool flip = !framebuffer;
+        const size_t src_stride = 4 * static_cast<size_t>(width);
+        const size_t dst_stride = 4 * static_cast<size_t>(ow);
+        const size_t src_col = 4 * static_cast<size_t>(x0 - x);
+        std::vector<uint8_t> overlap(dst_stride * static_cast<size_t>(oh));
+        for (int64_t r = 0; r < oh; ++r) {
+          const int64_t gl_row = flip ? y0 + oh - 1 - r : y0 + r;
+          std::copy_n(data + static_cast<size_t>(gl_row - y) * src_stride +
+                          src_col,
+                      dst_stride,
+                      overlap.data() + static_cast<size_t>(r) * dst_stride);
+        }
+        const SkImageInfo overlap_info = SkImageInfo::Make(
+            static_cast<int>(ow), static_cast<int>(oh), kRGBA_8888_SkColorType,
+            kUnpremul_SkAlphaType);
+        StaticBitmapImage::ShuffleSubchannelColorData(
+            overlap.data(), overlap_info, static_cast<int>(x0),
+            static_cast<int>(flip ? fb_height - y0 - oh : y0));
+        for (int64_t r = 0; r < oh; ++r) {
+          const int64_t gl_row = flip ? y0 + oh - 1 - r : y0 + r;
+          std::copy_n(overlap.data() + static_cast<size_t>(r) * dst_stride,
+                      dst_stride,
+                      data + static_cast<size_t>(gl_row - y) * src_stride +
+                          src_col);
+        }
       }
-
-      SkImageInfo image_info = SkImageInfo::Make(width, height, color_type, alpha_type);
-
-      // 调用指纹保护函数修改已读取的像素数据
-      StaticBitmapImage::ShuffleSubchannelColorData(data, image_info, 0, 0);
     }
   }
 }
diff --git a/third_party/blink/renderer/platform/fonts/font_cache.cc b/third_party/blink/renderer/platform/fonts/font_cache.cc
index 10a51bc..65fd51e 100644
--- a/third_party/blink/renderer/platform/fonts/font_cache.cc
+++ b/third_party/blink/renderer/platform/fonts/font_cache.cc
@@ -316,6 +316,13 @@ const FontPlatformData* FontCache::GetFontPlatformData(
       spoofed_platform = current_os;
     }
 
+    // Same platform as the real OS: installed fonts are that platform's real
+    // fonts, so none are hidden (a real Mac never lacks built-in fonts).
+    if (spoofed_platform == current_os) {
+      return font_platform_data_cache_.GetOrCreateFontPlatformData(
+          this, font_description, creation_params, alternate_font_name);
+    }
+
     std::vector<std::string> current_os_fonts = GetPlatformFonts(current_os);
     std::vector<std::string> platform_fonts = GetPlatformFonts(spoofed_platform);
 
@@ -340,12 +347,6 @@ const FontPlatformData* FontCache::GetFontPlatformData(
         }
         // 5%概率显示，继续默认流程
       }
-    } else {
-      // 系统相同，随机隐藏2%的字体以生成指纹
-      float probability = static_cast<float>(hash) / static_cast<float>(std::numeric_limits<uint32_t>::max());
-      if (probability < 0.02) {
-        return nullptr;
-      }
     }
   }
 
diff --git a/third_party/blink/renderer/platform/graphics/image_data_buffer.cc b/third_party/blink/renderer/platform/graphics/image_data_buffer.cc
index 4953725..0159341 100644
--- a/third_party/blink/renderer/platform/graphics/image_data_buffer.cc
+++ b/third_party/blink/renderer/platform/graphics/image_data_buffer.cc
@@ -32,11 +32,8 @@
 
 #include "third_party/blink/renderer/platform/graphics/image_data_buffer.h"
 
-#include "base/command_line.h"
 #include "base/compiler_specific.h"
 #include "base/memory/ptr_util.h"
-#include "components/ungoogled/ungoogled_switches.h"
-#include "third_party/blink/renderer/platform/graphics/static_bitmap_image.h"
 #include "third_party/blink/renderer/platform/image-encoders/image_encoder_utils.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 #include "third_party/blink/renderer/platform/wtf/text/base64.h"
@@ -134,13 +131,6 @@ base::span<const uint8_t> ImageDataBuffer::PixelData() const {
 bool ImageDataBuffer::EncodeImage(const ImageEncodingMimeType mime_type,
                                   const double& quality,
                                   Vector<unsigned char>* encoded_image) const {
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("canvas") == std::string::npos)) {
-    // shuffle subchannel color data within the pixmap
-    StaticBitmapImage::ShuffleSubchannelColorData(pixmap_.writable_addr(), pixmap_.info(), 0, 0);
-  }
   return ImageEncoder::Encode(encoded_image, pixmap_, mime_type, quality);
 }
 
diff --git a/third_party/blink/renderer/platform/graphics/static_bitmap_image.cc b/third_party/blink/renderer/platform/graphics/static_bitmap_image.cc
index e8df825..1fada36 100644
--- a/third_party/blink/renderer/platform/graphics/static_bitmap_image.cc
+++ b/third_party/blink/renderer/platform/graphics/static_bitmap_image.cc
@@ -4,6 +4,8 @@
 
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image.h"
 
+#include <functional>
+#include <string>
 #include <unordered_set>
 #include <vector>
 #include "base/command_line.h"
@@ -123,296 +125,103 @@ void StaticBitmapImage::DrawHelper(cc::PaintCanvas* canvas,
                         ToSkiaRectConstraint(draw_options.clamping_mode));
 }
 
-// set the component to maximum-delta if it is >= maximum, or add to existing color component (color + delta)
-#define shuffleComponent(color, max, delta) ( (color) >= (max) ? ((max)-(delta)) : ((color)+(delta)) )
+namespace {
 
-#define writable_addr(T, p, stride, x, y) (T*)((const char *)p + y * stride + x * sizeof(T))
+uint64_t SplitMix64(uint64_t z) {
+  z += 0x9e3779b97f4a7c15ULL;
+  z = (z ^ (z >> 30)) * 0xbf58476d1ce4e5b9ULL;
+  z = (z ^ (z >> 27)) * 0x94d049bb133111ebULL;
+  return z ^ (z >> 31);
+}
 
-void StaticBitmapImage::ShuffleSubchannelColorData(const void *addr, const SkImageInfo& info, int srcX, int srcY) {
-  auto w = info.width() - srcX, h = info.height() - srcY;
+// True if neighbour |n| is present (alpha != 0) and differs from |p| in alpha
+// or in the RGB bits above the least significant bit.
+bool DiffersFromNeighbour(const uint8_t* p, const uint8_t* n) {
+  if (n[3] == 0) {
+    return false;
+  }
+  return ((p[0] ^ n[0]) & 0xFE) || ((p[1] ^ n[1]) & 0xFE) ||
+         ((p[2] ^ n[2]) & 0xFE) || p[3] != n[3];
+}
 
+}  // namespace
+
+// Deterministic canvas noise for --fingerprint: sets the RGB least significant
+// bits of a seed-selected 1-in-8 subset of opaque, non-black/white edge pixels.
+// - Only 8-bit RGBA buffers (unpremultiplied or opaque) are touched, with an
+//   explicit 4-byte layout and |info.minRowBytes()| stride.
+// - Selection uses the absolute canvas coordinates (x + srcX, y + srcY), and
+//   eligibility only alpha and the RGB bits above the LSB, so applying the
+//   noise twice gives the same bytes and uniform images stay byte-identical.
+// - Neighbours outside the buffer or with alpha 0 count as absent, so the
+//   zero-initialized out-of-canvas part of an oversized getImageData behaves
+//   like the canvas edge.
+void StaticBitmapImage::ShuffleSubchannelColorData(const void* addr,
+                                                   const SkImageInfo& info,
+                                                   int srcX,
+                                                   int srcY) {
+  const int w = info.width();
+  const int h = info.height();
   // skip tiny images; info.width()/height() can also be 0
-  if ((w < 8) || (h < 8)) {
+  if (!addr || w < 8 || h < 8) {
     return;
   }
-
-  std::string seed_str = "0";
-  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
-  if (command_line->HasSwitch(switches::kFingerprint) &&
-      (!command_line->HasSwitch(switches::kDisableSpoofing) ||
-       command_line->GetSwitchValueASCII(switches::kDisableSpoofing).find("canvas") == std::string::npos)) {
-      seed_str = command_line->GetSwitchValueASCII(switches::kFingerprint);
+  if (info.colorType() != kRGBA_8888_SkColorType ||
+      (info.alphaType() != kUnpremul_SkAlphaType &&
+       info.alphaType() != kOpaque_SkAlphaType)) {
+    return;
   }
 
-  auto colorType = info.colorType();
-  auto fRowBytes = info.minRowBytes(); // stride
-
-  DLOG(INFO) << "BRM: ShuffleSubchannelColorData() w=" << w << " h=" << h << " colorType=" << colorType << " fRowBytes=" << fRowBytes;
-
-  auto max_pixels = (w * h) / 128;
-  if (max_pixels > 10) {
-    max_pixels = 10;
-  } else if (max_pixels < 2) {
-    max_pixels = 2;
+  const base::CommandLine* command_line = base::CommandLine::ForCurrentProcess();
+  if (!command_line->HasSwitch(switches::kFingerprint) ||
+      (command_line->HasSwitch(switches::kDisableSpoofing) &&
+       command_line->GetSwitchValueASCII(switches::kDisableSpoofing)
+               .find("canvas") != std::string::npos)) {
+    return;
   }
-
-  int modified_pixels = 0;
-  std::unordered_set<int> processed_rows;
-  std::unordered_set<int> processed_cols;
-
-  // 扫描所有像素，找出边缘像素并直接处理
-  for (int y = 0; y < h - 1 && modified_pixels < max_pixels; y++) {  // 排除最后一行
-    if (processed_rows.count(y)) continue;  // 跳过已处理的行
-    for (int x = 0; x < w - 1 && modified_pixels < max_pixels; x++) {  // 排除最后一列
-      if (processed_cols.count(x)) continue;  // 跳过已处理的列
-      bool isEdge = false;
-      bool isValidColor = false;
-
-      // 检查当前像素是否为边缘像素，并且不是纯黑或纯白
-      switch (colorType) {
-        case kRGBA_8888_SkColorType:
-        {
-          auto *current = writable_addr(uint32_t, addr, fRowBytes, x, y);
-          auto *right = writable_addr(uint32_t, addr, fRowBytes, x + 1, y);
-          auto *bottom = writable_addr(uint32_t, addr, fRowBytes, x, y + 1);
-
-          auto r = SkGetPackedR32(*current);
-          auto g = SkGetPackedG32(*current);
-          auto b = SkGetPackedB32(*current);
-
-          // 检查是否不是纯黑或纯白
-          isValidColor = !((r == 0 && g == 0 && b == 0) || (r == 255 && g == 255 && b == 255));
-
-          // 检查是否为边缘（与右侧或下侧像素不同）
-          if (isValidColor) {
-            isEdge = (*current != *right) || (*current != *bottom);
-          }
-
-          if (isEdge && isValidColor) {
-            // 直接处理这个像素
-            std::string pixel_key_base = seed_str + "_x" + std::to_string(x) + "_y" + std::to_string(y);
-            std::string pixel_key_r = pixel_key_base + "_r";
-            std::string pixel_key_g = pixel_key_base + "_g";
-            std::string pixel_key_b = pixel_key_base + "_b";
-
-            uint8_t shuffleR = std::hash<std::string>{}(pixel_key_r) & 1;
-            uint8_t shuffleG = std::hash<std::string>{}(pixel_key_g) & 1;
-            uint8_t shuffleB = std::hash<std::string>{}(pixel_key_b) & 1;
-
-            auto a = SkGetPackedA32(*current);
-            r = (r & ~0x1) | shuffleR;
-            g = (g & ~0x1) | shuffleG;
-            b = (b & ~0x1) | shuffleB;
-
-            *current = (a << SK_A32_SHIFT) | (r << SK_R32_SHIFT) |
-                      (g << SK_G32_SHIFT) | (b << SK_B32_SHIFT);
-            modified_pixels++;
-            processed_rows.insert(y);
-            processed_cols.insert(x);
-          }
-        }
-        break;
-
-        case kBGRA_8888_SkColorType:
-        {
-          auto *current = writable_addr(uint32_t, addr, fRowBytes, x, y);
-          auto *right = writable_addr(uint32_t, addr, fRowBytes, x + 1, y);
-          auto *bottom = writable_addr(uint32_t, addr, fRowBytes, x, y + 1);
-
-          auto b = SkGetPackedR32(*current);
-          auto g = SkGetPackedG32(*current);
-          auto r = SkGetPackedB32(*current);
-
-          // 检查是否不是纯黑或纯白
-          isValidColor = !((r == 0 && g == 0 && b == 0) || (r == 255 && g == 255 && b == 255));
-
-          // 检查是否为边缘（与右侧或下侧像素不同）
-          if (isValidColor) {
-            isEdge = (*current != *right) || (*current != *bottom);
-          }
-
-          if (isEdge && isValidColor) {
-            // 直接处理这个像素
-            std::string pixel_key_base = seed_str + "_x" + std::to_string(x) + "_y" + std::to_string(y);
-            std::string pixel_key_r = pixel_key_base + "_r";
-            std::string pixel_key_g = pixel_key_base + "_g";
-            std::string pixel_key_b = pixel_key_base + "_b";
-
-            uint8_t shuffleR = std::hash<std::string>{}(pixel_key_r) & 1;
-            uint8_t shuffleG = std::hash<std::string>{}(pixel_key_g) & 1;
-            uint8_t shuffleB = std::hash<std::string>{}(pixel_key_b) & 1;
-
-            auto a = SkGetPackedA32(*current);
-            r = (r & ~0x1) | shuffleR;
-            g = (g & ~0x1) | shuffleG;
-            b = (b & ~0x1) | shuffleB;
-
-            *current = (a << SK_BGRA_A32_SHIFT) | (r << SK_BGRA_R32_SHIFT) |
-                      (g << SK_BGRA_G32_SHIFT) | (b << SK_BGRA_B32_SHIFT);
-            modified_pixels++;
-            processed_rows.insert(y);
-            processed_cols.insert(x);
-          }
-        }
-        break;
-
-        case kGray_8_SkColorType:
-        {
-          auto *current = writable_addr(uint8_t, addr, fRowBytes, x, y);
-          auto *right = writable_addr(uint8_t, addr, fRowBytes, x + 1, y);
-          auto *bottom = writable_addr(uint8_t, addr, fRowBytes, x, y + 1);
-
-          isValidColor = (*current != 0 && *current != 255);
-          if (isValidColor) {
-            isEdge = (*current != *right) || (*current != *bottom);
-          }
-
-          if (isEdge && isValidColor) {
-            // 直接处理这个像素
-            std::string pixel_key_base = seed_str + "_x" + std::to_string(x) + "_y" + std::to_string(y);
-            std::string pixel_key_b = pixel_key_base + "_b";
-            uint8_t shuffleB = std::hash<std::string>{}(pixel_key_b) & 1;
-            *current = (*current & ~0x1) | shuffleB;
-            modified_pixels++;
-            processed_rows.insert(y);
-            processed_cols.insert(x);
-          }
-        }
-        break;
-
-        case kRGB_565_SkColorType:
-        {
-          auto *current = writable_addr(uint16_t, addr, fRowBytes, x, y);
-          auto *right = writable_addr(uint16_t, addr, fRowBytes, x + 1, y);
-          auto *bottom = writable_addr(uint16_t, addr, fRowBytes, x, y + 1);
-
-          unsigned r = SkPacked16ToR32(*current);
-          unsigned g = SkPacked16ToG32(*current);
-          unsigned b = SkPacked16ToB32(*current);
-
-          isValidColor = !((r == 0 && g == 0 && b == 0) || (r == 31 && g == 63 && b == 31));
-          if (isValidColor) {
-            isEdge = (*current != *right) || (*current != *bottom);
-          }
-
-          if (isEdge && isValidColor) {
-            // 直接处理这个像素
-            std::string pixel_key_base = seed_str + "_x" + std::to_string(x) + "_y" + std::to_string(y);
-            std::string pixel_key_r = pixel_key_base + "_r";
-            std::string pixel_key_g = pixel_key_base + "_g";
-            std::string pixel_key_b = pixel_key_base + "_b";
-
-            uint8_t shuffleR = std::hash<std::string>{}(pixel_key_r) & 1;
-            uint8_t shuffleG = std::hash<std::string>{}(pixel_key_g) & 1;
-            uint8_t shuffleB = std::hash<std::string>{}(pixel_key_b) & 1;
-
-            r = (r & ~0x1) | (shuffleR & 0x1);
-            g = (g & ~0x1) | (shuffleG & 0x1);
-            b = (b & ~0x1) | (shuffleB & 0x1);
-
-            unsigned r16 = (r & SK_R16_MASK) << SK_R16_SHIFT;
-            unsigned g16 = (g & SK_G16_MASK) << SK_G16_SHIFT;
-            unsigned b16 = (b & SK_B16_MASK) << SK_B16_SHIFT;
-
-            *current = r16 | g16 | b16;
-            modified_pixels++;
-            processed_rows.insert(y);
-            processed_cols.insert(x);
-          }
-        }
-        break;
-
-        case kARGB_4444_SkColorType:
-        {
-          auto *current = writable_addr(uint16_t, addr, fRowBytes, x, y);
-          auto *right = writable_addr(uint16_t, addr, fRowBytes, x + 1, y);
-          auto *bottom = writable_addr(uint16_t, addr, fRowBytes, x, y + 1);
-
-          auto r = SkGetPackedR4444(*current);
-          auto g = SkGetPackedG4444(*current);
-          auto b = SkGetPackedB4444(*current);
-
-          isValidColor = !((r == 0 && g == 0 && b == 0) || (r == 15 && g == 15 && b == 15));
-          if (isValidColor) {
-            isEdge = (*current != *right) || (*current != *bottom);
-          }
-
-          if (isEdge && isValidColor) {
-            // 直接处理这个像素
-            std::string pixel_key_base = seed_str + "_x" + std::to_string(x) + "_y" + std::to_string(y);
-            std::string pixel_key_r = pixel_key_base + "_r";
-            std::string pixel_key_g = pixel_key_base + "_g";
-            std::string pixel_key_b = pixel_key_base + "_b";
-
-            uint8_t shuffleR = std::hash<std::string>{}(pixel_key_r) & 1;
-            uint8_t shuffleG = std::hash<std::string>{}(pixel_key_g) & 1;
-            uint8_t shuffleB = std::hash<std::string>{}(pixel_key_b) & 1;
-
-            auto a = SkGetPackedA4444(*current);
-            r = (r & ~0x1) | (shuffleR & 0x1);
-            g = (g & ~0x1) | (shuffleG & 0x1);
-            b = (b & ~0x1) | (shuffleB & 0x1);
-
-            unsigned a4 = (a & 0xF) << SK_A4444_SHIFT;
-            unsigned r4 = (r & 0xF) << SK_R4444_SHIFT;
-            unsigned g4 = (g & 0xF) << SK_G4444_SHIFT;
-            unsigned b4 = (b & 0xF) << SK_B4444_SHIFT;
-
-            *current = r4 | b4 | g4 | a4;
-            modified_pixels++;
-            processed_rows.insert(y);
-            processed_cols.insert(x);
-          }
-        }
-        break;
-
-        case kAlpha_8_SkColorType:
-        {
-          auto *current = writable_addr(uint8_t, addr, fRowBytes, x, y);
-          auto *right = writable_addr(uint8_t, addr, fRowBytes, x + 1, y);
-          auto *bottom = writable_addr(uint8_t, addr, fRowBytes, x, y + 1);
-
-          auto r = SkColorGetR(*current);
-          auto g = SkColorGetG(*current);
-          auto b = SkColorGetB(*current);
-
-          isValidColor = !((r == 0 && g == 0 && b == 0) || (r == 255 && g == 255 && b == 255));
-          if (isValidColor) {
-            isEdge = (*current != *right) || (*current != *bottom);
-          }
-
-          if (isEdge && isValidColor) {
-            // 直接处理这个像素
-            std::string pixel_key_base = seed_str + "_x" + std::to_string(x) + "_y" + std::to_string(y);
-            std::string pixel_key_r = pixel_key_base + "_r";
-            std::string pixel_key_g = pixel_key_base + "_g";
-            std::string pixel_key_b = pixel_key_base + "_b";
-
-            uint8_t shuffleR = std::hash<std::string>{}(pixel_key_r) & 1;
-            uint8_t shuffleG = std::hash<std::string>{}(pixel_key_g) & 1;
-            uint8_t shuffleB = std::hash<std::string>{}(pixel_key_b) & 1;
-
-            auto a = SkColorGetA(*current);
-            r = (r & ~0x1) | shuffleR;
-            g = (g & ~0x1) | shuffleG;
-            b = (b & ~0x1) | shuffleB;
-
-            *current = SkColorSetARGB(a, r, g, b);
-            modified_pixels++;
-            processed_rows.insert(y);
-            processed_cols.insert(x);
-          }
-        }
-        break;
-
-        default:
-          break;
+  const uint64_t key = SplitMix64(std::hash<std::string>{}(
+      command_line->GetSwitchValueASCII(switches::kFingerprint) +
+      "canvas-pixels"));
+
+  const size_t row_bytes = info.minRowBytes();
+  uint8_t* pixels = static_cast<uint8_t*>(const_cast<void*>(addr));
+
+  for (int y = 0; y < h; ++y) {
+    uint8_t* row = pixels + static_cast<size_t>(y) * row_bytes;
+    uint8_t* next_row = (y + 1 < h) ? row + row_bytes : nullptr;
+    const uint64_t key_y = key ^ static_cast<uint64_t>(static_cast<uint32_t>(
+                                     static_cast<int64_t>(y) + srcY));
+    for (int x = 0; x < w; ++x) {
+      const uint64_t z = SplitMix64(
+          key_y ^ (static_cast<uint64_t>(static_cast<uint32_t>(
+                       static_cast<int64_t>(x) + srcX))
+                   << 32));
+      if ((z & 7) != 0) {
+        continue;
+      }
+      uint8_t* p = row + static_cast<size_t>(x) * 4;
+      if (p[3] != 255) {
+        continue;
       }
+      const int r = p[0] & 0xFE;
+      const int g = p[1] & 0xFE;
+      const int b = p[2] & 0xFE;
+      if ((r == 0 && g == 0 && b == 0) ||
+          (r == 0xFE && g == 0xFE && b == 0xFE)) {
+        continue;
+      }
+      const bool edge =
+          (x + 1 < w && DiffersFromNeighbour(p, p + 4)) ||
+          (next_row &&
+           DiffersFromNeighbour(p, next_row + static_cast<size_t>(x) * 4));
+      if (!edge) {
+        continue;
+      }
+      p[0] = static_cast<uint8_t>((p[0] & 0xFE) | ((z >> 8) & 1));
+      p[1] = static_cast<uint8_t>((p[1] & 0xFE) | ((z >> 9) & 1));
+      p[2] = static_cast<uint8_t>((p[2] & 0xFE) | ((z >> 10) & 1));
     }
   }
 }
 
-#undef writable_addr
-#undef shuffleComponent
-
 }  // namespace blink
